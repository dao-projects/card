

```
$ mkdir card && cd card
$ npm init
$ tsc --init

$ npm run build

$ npm adduser

//–access=public公共的，不加默认发布为私有，私有是收费的
$  npm publish --access=public 



//本地测试
$ npm link（添加命令行模式）
$ npm unlink（取消命令行模式）
//如何发布
$ npm adduser（用户名、密码、邮箱）-- 注册帐号
$ npm whoami（查看当前帐号）
$ npm publish（发布到npmjs.org）-- 注意：sudo权限
//如何卸载
$ npm unpublish npmsese --force
```
